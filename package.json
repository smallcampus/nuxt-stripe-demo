{
  "name": "stripe-demo",
  "version": "1.0.0",
  "description": "My bee&#39;s knees Nuxt.js project",
  "author": "Tin Mak",
  "private": true,
  "scripts": {
    "dev": "echo 'Please enter StripeClientId:'; read STRIPE_CLIENT_ID; cross-env NODE_ENV=development STRIPE_CLIENT_ID=$STRIPE_CLIENT_ID nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "cross-env": "^5.2.0",
    "koa": "^2.6.2",
    "nuxt": "^2.0.0",
    "nuxt-buefy": "^0.3.2",
    "nuxt-stripe": "^0.0.3",
    "nuxt-stripe-module": "^2.0.0"
  },
  "devDependencies": {
    "nodemon": "^1.18.9"
  }
}
